\nfrom selenium import webdriver\nfrom selenium.webdriver.common.by import By\nfrom selenium.webdriver.chrome.service import Service\nfrom webdriver_manager.chrome import ChromeDriverManager\n\n# Function to test interaction leads to survey\n\ndef test_tool_navigation_to_survey():\n    driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()))\n    try:\n        driver.get("http://platform-url.com")\n        assert "Platform" in driver.title\n        driver.find_element(By.ID, "needs-identification").click()  # Click tool\n        assert "Survey" in driver.title  # Check if survey opened\n        print("Survey opened successfully upon interaction.")\n    finally:\n        driver.quit()\n