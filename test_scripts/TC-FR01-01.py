<br>from selenium import webdriver<br>from selenium.webdriver.common.by import By<br>from selenium.webdriver.chrome.service import Service<br>from webdriver_manager.chrome import ChromeDriverManager<br>import time<br><br>driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()))<br>try:<br>    driver.get('URL_OF_PLATFORM')<br>    assert "Landing Page Title" in driver.title<br>    driver.find_element(By.ID, 'customer-interaction-btn').click()<br>    assert driver.find_element(By.ID, 'interaction-interface').is_displayed()<br>except Exception as e:<br>    print(e)<br>finally:<br>    driver.quit()