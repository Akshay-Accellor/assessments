\nfrom selenium import webdriver\nfrom selenium.webdriver.common.by import By\nfrom selenium.webdriver.chrome.service import Service\nfrom webdriver_manager.chrome import ChromeDriverManager\n\n# Function to test unauthenticated access\n\ndef test_unauthenticated_access():\n    driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()))\n    try:\n        driver.get("http://platform-url.com")\n        assert "Platform" in driver.title  # Check title\n        assert not driver.find_element(By.ID, "customer-interaction").is_displayed()  # Check absence\n        print("Unauthenticated access is restricted from interaction features.")\n    finally:\n        driver.quit()\n