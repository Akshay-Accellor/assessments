\nfrom selenium import webdriver\nfrom selenium.webdriver.common.by import By\nfrom selenium.webdriver.chrome.service import Service\nfrom webdriver_manager.chrome import ChromeDriverManager\n\n# Function to test needs identification tool visibility\n\ndef test_needs_identification_tool():\n    driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()))\n    try:\n        driver.get("http://platform-url.com")\n        assert "Platform" in driver.title  # Check title\n        assert driver.find_element(By.ID, "needs-identification").is_displayed()  # Check tool visibility\n        print("Needs identification tool is displayed successfully.")\n    finally:\n        driver.quit()\n